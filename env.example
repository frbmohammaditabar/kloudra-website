# Copy this file to .env and fill in real values. Never commit .env itself.

# A Gmail address to send application notifications FROM (use an App Password, not your normal password)
SMTP_USER=faribadevopsprojects@gmail.com
SMTP_PASS=your-16-character-app-password

# Where application emails should be delivered TO (usually the same address)
TO_EMAIL=faribadevopsprojects@gmail.com
